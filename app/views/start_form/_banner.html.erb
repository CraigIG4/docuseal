<%# IGSIGN — replaces DocuSeal banner in start_form views %>
<div style="text-align:center; margin-bottom:8px;">
  <%= render 'shared/logo', dark: false, height: '36' %>
  <div style="font-size:11px; font-weight:700; color:#7B787F; letter-spacing:0.08em; text-transform:uppercase; margin-top:6px;">
    Secure e-Signing
  </div>
</div>
